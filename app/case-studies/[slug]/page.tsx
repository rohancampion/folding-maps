import { notFound } from 'next/navigation';import Link from 'next/link';import { ArrowLeft,ArrowRight } from 'lucide-react';import { cases } from '@/lib/content';
export function generateStaticParams(){return cases.map(x=>({slug:x.slug}))}
export default async function CaseDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=cases.find(x=>x.slug===slug);if(!c)notFound();return <><article className="detail"><Link className="back" href="/case-studies"><ArrowLeft size={16}/> All case studies</Link><span className="badge">{c.status}</span><span className="kicker">{c.sector}</span><h1>{c.title}</h1><p className="lede">{c.summary}</p><div className="detail-visual"><div className="case-shape"/></div><div className="detail-grid"><h2>The challenge</h2><p>{c.challenge}</p><h2>Our work</h2><p>{c.work}</p><h2>The result</h2><p>{c.result}</p></div>{c.status==='Illustrative'&&<aside>This illustrative example shows the kind of engagement quiet gears can deliver. It does not represent a completed client project or measured client result.</aside>}</article><section className="cta-band"><h2>Where is friction<br/>holding you back?</h2><Link className="button lime" href="/contact">Talk it through with us <ArrowRight size={17}/></Link></section></>}
