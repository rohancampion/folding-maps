import type { MetadataRoute } from 'next';import {articles,cases} from '@/lib/content';export default function sitemap():MetadataRoute.Sitemap{const base='https://quietgears.co.uk';return ['','/about','/case-studies','/news','/contact','/privacy','/terms','/accessibility',...cases.map(x=>`/case-studies/${x.slug}`),...articles.map(x=>`/news/${x.slug}`)].map(url=>({url:base+url,lastModified:new Date()}))}
