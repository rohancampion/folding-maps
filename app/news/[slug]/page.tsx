import { articles } from '@/lib/content';import { notFound } from 'next/navigation';import Link from 'next/link';import { ArrowLeft } from 'lucide-react';
const bodies:Record<string,string[]>={
'open-weight-price-war':['The economics of artificial intelligence are shifting quickly. A new generation of open-weight models, many developed by Chinese laboratories, is competing on capability while driving inference costs down.','For an SME, this matters less as a story about model rankings and more as a change in what is commercially possible. Classification, document processing, retrieval and structured drafting can now be tested at costs that would have looked unrealistic only recently.','Low token prices do not make a business case by themselves. Integration, evaluation, security and adoption still demand careful work. The opportunity is to redirect budget from simply accessing intelligence towards designing the system around it.','Leaders should avoid betting their operating model on one provider. Build around clear tasks, maintain evaluation sets and keep the model layer replaceable. In a price war, flexibility is an asset.'],
'automation-before-agents':['AI agents are often presented as ready-made digital employees. In practice, their reliability depends on the process around them. A vague workflow produces vague automation.','Begin by following the work. Where does information arrive? Which decisions rely on judgement? What exceptions stop progress? Who is accountable when the answer is uncertain? That map is more valuable than a long list of possible tools.','Conventional automation will often handle deterministic steps better and more cheaply. Use AI where language, variation or interpretation makes it useful, and preserve clear checkpoints for consequential actions.','The best first system is rarely the most autonomous. It is the one your team can understand, trust and improve.'],
'cold-chain-collaboration':['quiet gears has begun a collaboration focused on information flow in temperature-controlled operations.','Cold-chain work creates large volumes of operational data, but value depends on turning that data into clear, timely action. Our early work is exploring how focused software and AI-supported reporting can reduce manual consolidation while preserving accountability.','The engagement reflects our approach: understand the operational reality first, then choose the smallest useful combination of software, automation and AI.'],
'small-teams-ai-advantage':['Large organisations may have more data and capital, but smaller firms possess advantages that matter when adopting AI: shorter decision lines, less legacy infrastructure and close knowledge of the customer.','The constraint is focus. Trying tools across every department creates activity without capability. A better approach is to choose one valuable workflow, define the desired change and give a small cross-functional group authority to deliver it.','Success creates reusable foundations: better data practices, a clearer view of risk and colleagues who understand how to work with the technology. Those foundations compound.','Small teams do not need to imitate an enterprise transformation programme. They need to learn faster than the market around them.']};
export function generateStaticParams(){return articles.map(x=>({slug:x.slug}))}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();return <article className="article-detail"><Link className="back" href="/news"><ArrowLeft size={16}/> All news</Link><span className="kicker">{a.tag} · {a.date} · {a.read}</span><h1>{a.title}</h1><p className="lede">{a.intro}</p><div className="article-hero-art"><i/><i/><i/></div><div className="prose">{bodies[slug].map((p,i)=><p key={i}>{p}</p>)}</div></article>}
