import Image from 'next/image';
import Link from 'next/link';
import { ArrowDown, ArrowUpRight } from 'lucide-react';
import { articles } from '@/lib/content';
export const metadata = { title: 'News' };
const topics = ['AI security', 'Practical AI', 'B2B strategy', 'Industry systems'];
export default function News() { return <><section className="page-hero news-hero"><div><span className="kicker">News, field notes & practical guides</span><h1>Ideas worth taking<br/><em>into the room.</em></h1><p>Detailed, sourced guidance for leaders turning fast-moving AI into secure, useful systems. Start with the question closest to your business, then follow the evidence.</p><div className="topic-list">{topics.map(topic => <span key={topic}>{topic}</span>)}</div><a className="news-scroll" href="#latest">Explore {articles.length} articles <ArrowDown size={16}/></a></div><aside><span>Now reading</span><strong>{articles[0].title}</strong><Link href={`/news/${articles[0].slug}`}>Open the latest briefing <ArrowUpRight size={16}/></Link></aside></section><section className="news-grid" id="latest">{articles.map((article,index)=><Link className={`article-card ${index===0?'featured':''}`} href={`/news/${article.slug}`} key={article.slug}><div className={`article-art art-${index}`}><Image src={article.image} alt="" fill sizes={index===0?'(max-width: 800px) 100vw, 55vw':'(max-width: 800px) 100vw, 45vw'}/><span>{article.artLabel}</span></div><div className="article-copy"><div><span>{article.tag}</span><span>{article.date} · {article.read}</span></div><h2>{article.title}</h2><p>{article.intro}</p><b>Read article <ArrowUpRight size={15}/></b></div></Link>)}</section></>; }
