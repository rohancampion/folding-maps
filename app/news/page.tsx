import Image from 'next/image';import Link from 'next/link';import { ArrowUpRight } from 'lucide-react';import { articles } from '@/lib/content';
export const metadata={title:'News'};
export default function News(){return <><section className="page-hero"><span className="kicker">News & thinking</span><h1>Clear thinking on<br/><em>fast-moving technology.</em></h1><p>Notes from our work, analysis of the AI market and practical guidance for leaders making technology decisions.</p></section><section className="news-grid">{articles.map((a,i)=><Link className={`article-card ${i===0?'featured':''}`} href={`/news/${a.slug}`} key={a.slug}><div className={`article-art art-${i}`}><Image src={a.image} alt="" fill sizes={i===0?'(max-width: 800px) 100vw, 55vw':'(max-width: 800px) 100vw, 45vw'}/><span>{a.artLabel}</span></div><div className="article-copy"><div><span>{a.tag}</span><span>{a.date} · {a.read}</span></div><h2>{a.title}</h2><p>{a.intro}</p><b>Read article <ArrowUpRight size={15}/></b></div></Link>)}</section></>}
